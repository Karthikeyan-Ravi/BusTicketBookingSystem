@model BusTicketBookingSystem.Models.SearchBus

@* @{
    ViewData["Title"] = "Home";
}
 *@
@* <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <title>@ViewData["Title"]</title>
</head> *@
<script type="text/javascript">
    // var today = new Date();
    // console.log(today);
    //document.getElementById('travelDate').value =  "<?php echo date("Y-m-d"); ?>";
</script>
<body>
    <div class="container">
        <div class="search-section mx-auto">
            <h2 class="text-center mb-4">Find Your Next Trip!</h2>
            <form asp-action="SearchBus" method="post">
                <div class="mb-3">
                    <label for="source" class="form-label">Source</label>
                    <input type="text" class="form-control" id="source" placeholder="Enter Source City" name="Source" asp-for="Source" required>
                </div>
                <div class="mb-3">
                    <label for="destination" class="form-label">Destination</label>
                    <input type="text" class="form-control" id="destination" placeholder="Enter Destination City" name="Destination" asp-for="Destination" required>
                </div>
                <div class="mb-3">
                    <label for="travelDate" class="form-label">Travel Date</label>
                <input type="date" class="form-control" id="travelDate" value='<?php echo date(" Y-m-d"); ?>' name="TravelDate" asp-for="TravelDate" required>
                </div>
                <button type="submit" class="btn btn-search w-100">Search</button>
            </form>
        </div>
    </div>
</body>
<style type="text/css">
    .search-bus-btn {
        margin-top: 20px;
    }

    .bus-home-container {
        width: 50%;
    }
    /* Custom Styles */
    body {
        background-color: #f8f9fa; /* Light gray background */
    }

    .navbar {
        background-color: #0d6efd; /* Dark blue */
    }

        .navbar a {
            color: white !important;
            text-decoration: none;
        }

    .search-section {
        margin-top: 50px;
        padding: 30px;
        background-color: white;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .btn-search {
        background-color: orange;
        color: white;
        font-weight: bold;
    }

        .btn-search:hover {
            background-color: darkorange;
        }

    /* .footer {
        margin-top: 50px;
        padding: 10px 0;
        background-color: #0d6efd;
        color: white;
        text-align: center;
        display:  none;
    } */
</style>
